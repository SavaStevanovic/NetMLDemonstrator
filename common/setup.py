from setuptools import setup

setup(
   name='network-output-processor',
   version='0.0.1',
   description='Module to apply network output to images',
   install_requires=[
	'numpy',
	'pillow'
   ]
)
